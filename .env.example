# Public endpoint only; never add keys, tokens, connection strings, or tenant secrets.
COSMOS_ENDPOINT=https://db-cosmos-basic-test.documents.azure.com:443/
COSMOS_DATABASE=chatbot-test
COSMOS_CONTAINER=sessions
DOCUMENT_COUNT=100

# Optional: client ID of a user-assigned managed identity.
# AZURE_CLIENT_ID=
